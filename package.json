{
  "name": "trvernal-express",
  "main": "server.js",
  "scripts": {
    "prestart": "npm run build",
    "build": "./node_modules/.bin/babel server.js -o app",
    "dev": "nodemon --exec \"babel-node server\"",
    "start": "node app"
  },
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",    
    "babel-cli": "^6.26.0",    
    "compression": "^1.7.0",
    "express": "^4.15.4",
    "mailgun-js": "^0.13.1",
    "morgan": "^1.8.2",
    "pug": "^2.0.0-rc.3"
  },
  "devDependencies": {
    "dotenv": "^4.0.0",
    "nodemon": "^1.11.0"
  }
}
